# Desarrollo local (PostgreSQL)
DB_NAME=db_escuela
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

# Producción Render (usa la Internal Database URL)
# DATABASE_URL=postgresql://usuario:password@host/educontrol
# DATABASE_SSL=False

SECRET_KEY=genera-una-clave-segura-y-larga
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# URLs del frontend separadas por coma (sin barra final)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://tu-proyecto.vercel.app
